---
- name: Update package cache
  become: true
  apt:
    update_cache: yes

- name: Install required packages
  become: true
  package:
    name: "{{ item }}"
    state: present
  loop:
    - libpam-modules
    - openssh-server
    - update-motd
