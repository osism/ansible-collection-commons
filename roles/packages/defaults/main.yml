---
packages_upgrade: true

packages_required_default:
  - ethtool
  - jq
  - rsyslog

packages_required_extra: []
packages_required: "{{ packages_required_default + packages_required_extra + packages_required_distribution }}"
